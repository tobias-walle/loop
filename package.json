{
  "name": "loop",
  "version": "0.1.0",
  "description": "A CLI tool that runs coding agents in loops and sequences with a minimal TUI",
  "license": "MIT",
  "type": "module",
  "bin": {
    "loop": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": ["dist", "src/templates"],
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --outfile dist/cli.js --target node && bun build src/index.ts --outdir dist --target node && chmod +x dist/cli.js",
    "test": "bun test",
    "check": "sh scripts/check.sh",
    "check:biome": "biome check --write --unsafe . --error-on-warnings --max-diagnostics=50",
    "check:types": "tsc --noEmit --pretty",
    "check:test": "bun test --reporter=dots --bail --path-ignore-patterns='*.integration.*'",
    "test:integration": "bun test --bail 'integration'",
    "check:knip": "knip --no-config-hints",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "knip": "knip",
    "typecheck": "tsc --noEmit",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "@mariozechner/pi-tui": "^0.63.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "^1.3.11",
    "@types/node": "^22.0.0",
    "knip": "^5.0.0",
    "typescript": "^5.7.0"
  }
}
